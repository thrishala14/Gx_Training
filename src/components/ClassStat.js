function ClassStat(props) {
  //const unreadMessages = props.unreadMessages;
  const count = 0;
      return (
        <div>
          {count>0 && <h1>Messages: {count}</h1>}
        </div>
      );

}



export default ClassStat